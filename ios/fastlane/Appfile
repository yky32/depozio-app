# App Store Connect API credentials
# These are set via environment variables in GitHub Actions

app_identifier("com.depozio")
apple_id(ENV["APPLE_ID"] || "wayneyu.dev@gmail.com")
team_id(ENV["TEAM_ID"] || "3G34999H3A")

# App Store Connect API Key (recommended for CI/CD)
# Set via environment variables:
# - APP_STORE_CONNECT_API_KEY_ID
# - APP_STORE_CONNECT_ISSUER_ID
# - APP_STORE_CONNECT_API_KEY_PATH

